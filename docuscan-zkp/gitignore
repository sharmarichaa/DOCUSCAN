# Dependencies
node_modules/

# Large build artifacts (regenerated by setup.js — do NOT commit these)
build/

# Temp files created during proof generation (auto-deleted but just in case)
scripts/_tmp_*.json
scripts/_api_*.json

# Environment
.env
.env.local

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/

# NOTE: proofs/ folder IS committed — it contains public data only
# verification_key.json    — public key, safe to share
# registration_proof.json  — ZK proof, safe to share
# registrations.json       — hash commitments only, NO document content
# audit_log.json           — timestamps and results only
